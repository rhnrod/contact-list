class Contato {
  nome: string
  email: string
  telefone: number
  id: string

  constructor(nome: string, email: string, telefone: number, id: string) {
    this.nome = nome
    this.email = email
    this.telefone = telefone
    this.id = id
  }
}

export default Contato
